<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.sdpscramble">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.create.CreateScrambleActivity" />
        <activity android:name=".ui.play.PlayScrambleActivity" />
        <activity android:name=".ui.register.RegistrationActivity" />
        <activity android:name=".ui.scrambles.ScramblesActivity" />

        <provider
            android:name=".data.ScrambleProvider"
            android:authorities="@string/content_authority"
            android:exported="false"
            android:syncable="true" />

        <activity android:name=".ui.stats.PlayerStatActivity" />
        <activity android:name=".ui.stats.ScrambleStatActivity"/>

        <service
            android:name=".webservice.sync.ScrambleSyncIntentService"
            android:exported="false" />
    </application>

</manifest>